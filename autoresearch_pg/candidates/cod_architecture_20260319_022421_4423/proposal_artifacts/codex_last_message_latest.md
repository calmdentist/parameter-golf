Hypothesis: normalize encoder skip activations before decoder fusion so the existing per-channel `skip_weights` see a more stable signal, which may trim the architecture family’s post-quant gap without changing artifact bytes. I implemented that in [train_gpt_mlx.py](/Users/calmdentist/Desktop/parameter-golf/autoresearch_pg/candidates/cod_architecture_20260319_022421_4423/train_gpt_mlx.py#L81) and documented the proposal in [notes.md](/Users/calmdentist/Desktop/parameter-golf/autoresearch_pg/candidates/cod_architecture_20260319_022421_4423/notes.md#L3).

The code change adds a default-on `NORMALIZE_SKIP_CONNECTIONS=1` knob, applies `rms_norm(skip)` before `skip_weights` in the decoder skip path, and logs `normalize_skip_connections` for reproducibility in [train_gpt_mlx.py](/Users/calmdentist/Desktop/parameter-golf/autoresearch_pg/candidates/cod_architecture_20260319_022421_4423/train_gpt_mlx.py#L430). Expected upside is better export stability and lower `final_int8_zlib_roundtrip_exact val_bpb` at effectively unchanged bytes; the main risk is that raw skip magnitude was useful signal and this over-regularizes the decoder handoff.

`python3 -m py_compile train_gpt_mlx.py` passed. A live MLX runtime smoke test was blocked by a local Metal-device crash before model init, so I did not run training.